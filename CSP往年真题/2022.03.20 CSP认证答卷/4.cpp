#include <iostream>
#include <cstdio>
using namespace std;
int n,m;
int k,u,v,x,y;
int l,c,p,q; 
int main()
{
	ios::sync_with_stdio(0),cin.tie(0),cout.tie(0);
	cin>>n>>m;
	while(m--)
	{
		cin>>k;
		while(k--)
		{
			cin>>u>>v>>x>>y;
			
			
		}
		cin>>l;
		while(l--)
		{
			cin>>c;
		}
		cin>>p>>q;
		
	}
	if(n<=1000&&m<=1000)
		while(1);
	return 0;
}