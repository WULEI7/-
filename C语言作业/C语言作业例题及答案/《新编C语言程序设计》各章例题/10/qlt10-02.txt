#include <stdio.h>
struct student    /*在各函数外，定义的是外部结构体*/
{  int num;
   char *name;
   char sex;
   float score;
}stu2,stu1={102,"Zhang ping",'M',78.5}; /*按成员顺序逗号分隔，给出各成员常量值*/
main()
{  stu2=stu1;
   printf("Number=%d\nName=%s\nSex=%c\nScore=%f\n",
              stu2.num,stu2.name,stu2.sex,stu2.score);
}
