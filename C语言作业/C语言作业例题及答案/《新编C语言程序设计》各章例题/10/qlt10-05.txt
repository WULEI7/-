#include <stdio.h>
struct student
{  int num;
   char name[20];
   char sex;
   float score;
} stu1={102,"Zhang ping",'M',78.5},*pstu;
void main()
{  pstu=&stu1;
   printf("Number=%d\nName=%s\n",stu1.num,stu1.name); //结构体成员表示一
   printf("Sex=%c\nScore=%f\n\n",stu1.sex,stu1.score);
   printf("Number=%d\nName=%s\n",(*pstu).num,(*pstu).name);//成员表示二
   printf("Sex=%c\nScore=%f\n\n",(*pstu).sex,(*pstu).score);
   printf("Number=%d\nName=%s\n",pstu->num,pstu->name);    //成员表示三
   printf("Sex=%c\nScore=%f\n\n",pstu->sex,pstu->score);
}
