#include <stdio.h>
#include "string.h"
main()
{  int i;  char st1[]="...........is good.",st2[]="C Language";
   printf("Total bytes of st1 is %d, valid bytes is %d\n",sizeof(st1),strlen(st1));
   printf("Before strcpy st2 to st1,st1 is:  ");
   for(i=0;i<sizeof(st1);i++) printf("%c",st1[i]); printf("\n");
   printf("Every ascii of char in st1:\n");
   for(i=0;i<sizeof(st1);i++) printf("%d-",st1[i]); printf("\n");
   strcpy(st1,st2);
   printf("After strcpy st2 to st1,st1 is:  "); puts(st1); 
   printf("Total bytes  of st1 is %d, valid bytes is %d\n",sizeof(st1),strlen(st1));
   printf("Every char in st1:");
   for(i=0;i<sizeof(st1);i++) printf("%c",st1[i]);  printf("\n");
   printf("Every ascii of char in st1:\n");
   for(i=0;i<sizeof(st1);i++) printf("%d-",st1[i]);  printf("\n");
}
