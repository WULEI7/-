#include <stdio.h>
main()
{  int i,max,a[10];
   printf("input 10 numbers:\n");
   for(i=0;i<10;i++)
     scanf("%d",&a[i]);  //数组元素如变量，不忘加上&
   max=a[0];
   for(i=1;i<10;i++) if(a[i]>max) max=a[i]; //逐个比较获取最大值
   printf("maxmum=%d\n",max);
}
