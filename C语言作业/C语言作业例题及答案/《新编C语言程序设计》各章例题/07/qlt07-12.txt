#include <stdio.h>
main()
{  char st1[6],st2[6],st3[6],st4[6];
   printf("input string:\n");
   scanf("%s%s%s%s",st1,st2,st3,st4); //数组名即地址，注意这里数组名前不需要加&
   printf("%s %s %s %s\n",st1,st2,st3,st4);
}
