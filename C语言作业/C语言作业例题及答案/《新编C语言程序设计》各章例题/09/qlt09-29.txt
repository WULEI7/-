#include <stdio.h>
cpystr(char *pds, char *pss){
   while((*pds=*pss)!='\0'){
      pds++;   // 这里也可 ++pds;
      pss++;   // 这里也可 ++pss;
   }
}
main(){
   char *pa="CHINA",b[10],*pb; pb=b;
   cpystr(pb, pa);
   printf("string a=%s\nstring b=%s\n",pa,pb);
}
