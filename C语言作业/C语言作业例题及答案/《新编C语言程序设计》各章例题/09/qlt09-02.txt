#include <stdio.h>
main()
{  int *p1,*p2,*p,a,b;
   scanf("%d,%d",&a,&b);
   p1=&a;p2=&b;
   if(a<b) {p=p1;p1=p2;p2=p;}   //交换两指针变量的值，意味着p1,p2的指向改变了
   printf("\na=%d,b=%d\n",a,b); //a,b的值没变化
   printf("max=%d,min=%d\n",*p1, *p2); //p1,p2的指向可能改变了,p1指向了最大数
}
