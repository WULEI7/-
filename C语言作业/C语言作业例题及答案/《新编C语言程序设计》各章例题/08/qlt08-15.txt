#include <stdio.h>
fun(int a)
{  auto int b=0; 
   static int c=3;//c初始化一次，保留固定存储单元。3次调用时c的值分别是3、4、5
   b=b+1;
   c=c+1;
   return(a+b+c);
}
main()
{  int a=2,i;
   for(i=0;i<3;i++) printf("%d ",fun(a));
}
