#include <stdio.h>
void main(){
   int i=2,p;
   p=fun(i,++i);  //如果本语句换成p=fun(i,i++); 运行情况又如何？
   printf("i=%d,p=%d\n",i,p);
}
int fun(int a, int b)
{  int c; printf("a=%d,b=%d\n",a,b);
   if (a>b) c=1;
   else if (a==b) c=0;
   else c=-1;
   return (c);
}
